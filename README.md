# Grammophone.DataAccess.EntityFramework.Plus

Optional Entity Framework 6 integration for set-based operations backed by Entity Framework Plus.

Use `EFDomainContainerPlus` as the base class for EF6 domain containers that should support portable set-based query operations such as `ExecuteDelete` and `ExecuteDeleteAsync`.
